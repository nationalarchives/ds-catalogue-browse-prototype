{%- set containerClasses = [params.classes] if params.classes else [] -%}
{%- set classes = containerClasses | join(' ') -%}
<ul class="browse-filters{% if classes %} {{ classes }}{% endif %}" data-module="browse-filters"{% for attribute, value in params.attributes %} {{ attribute }}{% if value !== '' %}="{{ value }}"{% endif %}{% endfor %}>
  {%- for item in params.items %}
  <li class="browse-filters__item{% if item.selected %} browse-filters__item--selected{% endif %}"{% if item.filter %} data-filter="{{ item.filter }}"{% endif %}>
    <a href="{{ item.href }}" class="browse-filters__link">{{ item.label }}<span class="tna-visually-hidden browse-filters__state">{% if item.selected %}, remove filter{% endif %}</span></a>
    <span class="browse-filters__remove" aria-hidden="true"></span>
  </li>
  {%- endfor %}
</ul>
